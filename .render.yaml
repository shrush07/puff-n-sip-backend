services:
  - type: web
    name: backend
    buildCommand: |
      npm install
      npm run build  # Add the appropriate build command here
    startCommand: npm start
    runtime: node
    plan: free
